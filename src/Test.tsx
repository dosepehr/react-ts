import React from 'react';

const Test: React.FC = () => {
    return 1;
};

export default Test;

// JSX.Element ==> for functional component
// React.ReactNode ==> can be used for string,numbers,more for props.children
// React.FC ==> similar to ReactNode, intelisense for Test.[]
